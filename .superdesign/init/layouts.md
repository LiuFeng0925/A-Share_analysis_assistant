# 布局现状

`frontend/src/components/AppShell.tsx` 已实现左侧固定应用栏与右侧工作区：

- 桌面端左栏 208px、高度 100vh、只显示“全部股票”。
- 小于 860px 时变为 64px 顶部紧凑栏。
- 主内容由各页面控制，最大宽度 1600px；宽表格使用内部横向滚动。

详情页必须继续放在同一 `workspace` 内，不新增二级侧栏。

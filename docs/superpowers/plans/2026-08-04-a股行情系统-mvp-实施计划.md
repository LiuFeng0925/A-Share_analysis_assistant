# A 股行情系统 MVP 实施计划

> **面向代理开发者：** 必须使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`，严格按任务顺序逐项实施。所有步骤使用复选框跟踪。

**目标：** 构建一个本地优先的 A 股行情 MVP，支持全市场一分钟快照、股票搜索、个股详情、今日原生一分钟 K 线、多周期 K 线以及本地永久存储。

**架构：** 前端使用 React 与 TypeScript，后端使用 FastAPI。DuckDB 保存热数据与元数据，Parquet 保存按日期分区的长期行情；公开行情接口被隔离在数据源适配器内。前端每分钟轮询后端，Apache ECharts 负责 K 线、成交量和缩放交互。

**技术栈：** Python 3.12、FastAPI 0.141.1、DuckDB 1.5.5、AKShare 1.18.81、Pandas 3.0.5、PyArrow 25.0.0、React 19.2.8、React Router 7.18.2、ECharts 6.1.0、Vite 8.2.0、TypeScript 7.0.2、Vitest 4.1.10、Playwright 1.62.1。

## 全局约束

- 所有产品文案、日志中的描述性文本、README 和代码注释使用中文。
- 数据成本为零，只调用免费公开数据源。
- 第一版只显示“全部股票”一个左侧菜单项。
- 中国市场配色：红涨、绿跌。
- 交易时段按 `Asia/Shanghai`：09:30–11:30、13:00–15:00。
- 全市场行情快照每分钟最多采集一次；休市时不轮询。
- 全市场快照不得标记为原生分钟 K。
- 历史日 K 首次回补最近 60 个交易日。
- 免费原生 1 分钟 K 只保证数据源可提供的近期范围，取得后永久保存。
- 第一版不实现技术指标、筛选器、收藏、提醒、回测、登录或交易功能。
- 后端 MVP 只运行一个写进程；DuckDB 写入通过同一把锁串行执行。
- 普通自动化测试不得访问公网；在线烟雾测试必须显式启用。
- 每个任务必须遵守测试先行、最小实现、完整验证和独立提交。

---

## 文件结构与责任

```text
backend/
  pyproject.toml                         Python 依赖、测试与静态检查配置
  src/a_share_radar/
    __init__.py                          包版本
    config.py                            本地路径、时区和采集配置
    main.py                              FastAPI 应用工厂与生命周期
    domain/models.py                     股票、快照、K 线和质量状态模型
    data_sources/protocol.py             数据源协议
    data_sources/akshare_source.py       AKShare 字段规范化与公网调用
    data_sources/fixture_source.py       端到端测试固定数据源
    storage/database.py                  DuckDB 连接、建表和单写入锁
    storage/repository.py                股票、行情、K 线和状态查询
    storage/archive.py                   DuckDB 到 Parquet 的原子归档
    services/market_clock.py             交易日和交易时段判断
    services/snapshot_collector.py       全市场快照采集与校验
    services/bar_service.py              日 K 回补和按需分钟 K
    services/scheduler.py                一分钟采集调度与收盘归档调度
    api/schemas.py                       HTTP 请求与响应模型
    api/routes.py                        市场、股票、K 线和状态接口
  tests/
    conftest.py                          临时数据库和固定数据源夹具
    test_health.py                       应用健康检查
    test_akshare_source.py               数据源规范化
    test_repository.py                   DuckDB 幂等与查询
    test_snapshot_collector.py           快照校验与失败降级
    test_bar_service.py                  日 K 与分钟 K 行为
    test_archive.py                      Parquet 原子归档
    test_api.py                          HTTP 合约

frontend/
  package.json                           前端依赖和脚本
  pnpm-lock.yaml                         锁定依赖
  vite.config.ts                         Vite 与 Vitest 配置
  tsconfig.json                          TypeScript 配置
  playwright.config.ts                   端到端配置
  src/
    main.tsx                             React 入口
    app/App.tsx                          路由与页面壳
    app/styles.css                       HeroUI 浅色视觉变量与全局样式
    api/client.ts                        类型化 HTTP 客户端
    api/types.ts                         前后端数据类型
    hooks/usePolling.ts                  一分钟轮询与页面可见性控制
    components/AppShell.tsx              单菜单侧栏与顶部区域
    components/MarketSummary.tsx         市场概览卡片
    components/StockTable.tsx            一股一行行情表格
    components/KlineChart.tsx            ECharts K 线与成交量
    pages/StockListPage.tsx              列表、搜索、过滤和分页
    pages/StockDetailPage.tsx            个股行情和周期切换
    test/setup.ts                         测试环境
    test/fixtures.ts                     固定前端数据
    components/AppShell.test.tsx         导航约束
    pages/StockListPage.test.tsx         列表与搜索
    pages/StockDetailPage.test.tsx       今日和周期切换
    components/KlineChart.test.ts        图表配置与缩放
  e2e/mvp.spec.ts                        关键用户路径

scripts/
  bootstrap.sh                           创建虚拟环境并安装依赖
  dev.sh                                 一条命令启动前后端
  test.sh                                一条命令运行全部测试
  smoke_live.py                          显式在线接口烟雾测试

README.md                                中文运行、数据边界和排错说明
```

---

### Task 1｜任务 1：后端应用骨架与健康检查

**文件：**

- 创建：`backend/pyproject.toml`
- 创建：`backend/src/a_share_radar/__init__.py`
- 创建：`backend/src/a_share_radar/config.py`
- 创建：`backend/src/a_share_radar/main.py`
- 创建：`backend/tests/test_health.py`

**接口：**

- 产出：`Settings(data_dir: Path, database_path: Path, timezone: str)`
- 产出：`create_app(settings: Settings | None = None, source: MarketDataSource | None = None) -> FastAPI`
- 产出：`GET /api/health -> {"status": "ok"}`

- [ ] **步骤 1：创建 Python 项目配置和失败测试**

`backend/pyproject.toml` 使用以下精确依赖：

```toml
[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "a-share-radar"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.141.1",
  "uvicorn[standard]==0.52.1",
  "duckdb==1.5.5",
  "akshare==1.18.81",
  "pandas==3.0.5",
  "pyarrow==25.0.0",
  "pydantic==2.13.4",
  "pydantic-settings==2.14.2",
  "apscheduler==3.11.3",
]

[project.optional-dependencies]
dev = [
  "pytest==9.1.1",
  "pytest-asyncio==1.4.0",
  "httpx==0.28.1",
  "ruff==0.16.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/a_share_radar"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"
```

`backend/tests/test_health.py`：

```python
from httpx import ASGITransport, AsyncClient

from a_share_radar.main import create_app


async def test_health_returns_ok(tmp_path):
    app = create_app()
    async with AsyncClient(
        transport=ASGITransport(app=app), base_url="http://test"
    ) as client:
        response = await client.get("/api/health")

    assert response.status_code == 200
    assert response.json() == {"status": "ok"}
```

- [ ] **步骤 2：安装依赖并确认测试失败**

运行：

```bash
cd backend
python3 -m venv .venv
.venv/bin/pip install -e '.[dev]'
.venv/bin/pytest tests/test_health.py -q
```

预期：失败，错误包含 `No module named 'a_share_radar.main'`。

- [ ] **步骤 3：实现最小应用工厂**

`backend/src/a_share_radar/__init__.py`：

```python
__version__ = "0.1.0"
```

`backend/src/a_share_radar/config.py`：

```python
from pathlib import Path

from pydantic import computed_field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="A_SHARE_", env_file=".env")

    data_dir: Path = Path("data")
    timezone: str = "Asia/Shanghai"
    snapshot_interval_seconds: int = 60
    stale_after_seconds: int = 120
    history_days: int = 60
    history_request_delay_seconds: float = 0.5
    fixture_source: bool = False

    @computed_field
    @property
    def database_path(self) -> Path:
        return self.data_dir / "a_share_radar.duckdb"
```

`backend/src/a_share_radar/main.py`：

```python
from fastapi import FastAPI

from a_share_radar.config import Settings


def create_app(settings: Settings | None = None, source: object | None = None) -> FastAPI:
    app = FastAPI(title="A 股雷达", version="0.1.0")
    app.state.settings = settings or Settings()
    app.state.source = source

    @app.get("/api/health")
    async def health() -> dict[str, str]:
        return {"status": "ok"}

    return app


app = create_app()
```

- [ ] **步骤 4：运行测试和静态检查**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_health.py -q
.venv/bin/ruff check src tests
```

预期：`1 passed`，Ruff 输出 `All checks passed!`。

- [ ] **步骤 5：提交后端骨架**

```bash
git add backend
git commit -m "feat: 建立后端应用骨架"
```

---

### Task 2｜任务 2：领域模型与 AKShare 数据源适配器

**文件：**

- 创建：`backend/src/a_share_radar/domain/models.py`
- 创建：`backend/src/a_share_radar/data_sources/protocol.py`
- 创建：`backend/src/a_share_radar/data_sources/akshare_source.py`
- 创建：`backend/tests/test_akshare_source.py`

**接口：**

- 产出：`Market`、`QualityStatus`、`Stock`、`QuoteSnapshot`、`Bar`
- 产出：`MarketDataSource.fetch_stock_master()`
- 产出：`MarketDataSource.fetch_trading_days(start, end)`
- 产出：`MarketDataSource.fetch_market_snapshot()`
- 产出：`MarketDataSource.fetch_daily_bars(code, start, end, period, adjustment)`
- 产出：`MarketDataSource.fetch_minute_bars(code, start, end, period, adjustment)`
- 产出：`AkshareSource.normalize_snapshot(frame, captured_at)`

- [ ] **步骤 1：写规范化失败测试**

`backend/tests/test_akshare_source.py`：

```python
from datetime import datetime
from zoneinfo import ZoneInfo

import pandas as pd

from a_share_radar.data_sources.akshare_source import AkshareSource
from a_share_radar.domain.models import Market, QualityStatus


def test_normalize_snapshot_maps_chinese_columns():
    frame = pd.DataFrame(
        [
            {
                "代码": "600519",
                "名称": "贵州茅台",
                "最新价": 1330.06,
                "涨跌幅": -2.13,
                "涨跌额": -28.92,
                "今开": 1350.06,
                "最高": 1350.94,
                "最低": 1330.04,
                "昨收": 1358.98,
                "成交量": 33455,
                "成交额": 4472998836.0,
                "换手率": 0.27,
                "总市值": 1670000000000.0,
            }
        ]
    )
    captured_at = datetime(2026, 8, 4, 10, 31, tzinfo=ZoneInfo("Asia/Shanghai"))

    quotes = AkshareSource.normalize_snapshot(frame, captured_at)

    assert len(quotes) == 1
    assert quotes[0].market is Market.SH
    assert quotes[0].code == "600519"
    assert quotes[0].latest_price == 1330.06
    assert quotes[0].volume == 33455
    assert quotes[0].quality_status is QualityStatus.OK
```

再增加三个测试：

```python
def test_normalize_snapshot_turns_dash_into_none():
    frame = pd.DataFrame([{"代码": "000001", "名称": "平安银行", "最新价": "-"}])
    quotes = AkshareSource.normalize_snapshot(
        frame, datetime(2026, 8, 4, 10, 31, tzinfo=ZoneInfo("Asia/Shanghai"))
    )
    assert quotes[0].latest_price is None
    assert quotes[0].quality_status is QualityStatus.PARTIAL


def test_market_mapping_supports_beijing_exchange():
    assert AkshareSource.market_for_code("920092") is Market.BJ


def test_minute_bar_keeps_provider_time():
    frame = pd.DataFrame(
        [{"时间": "2026-08-04 10:31:00", "开盘": 10.1, "收盘": 10.2,
          "最高": 10.3, "最低": 10.0, "成交量": 1000, "成交额": 10200.0}]
    )
    bars = AkshareSource.normalize_minute_bars("000001", frame, "1m", "none")
    assert bars[0].bar_time.isoformat() == "2026-08-04T10:31:00+08:00"
    assert bars[0].is_complete is True
```

- [ ] **步骤 2：运行测试确认缺少模型和适配器**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_akshare_source.py -q
```

预期：收集阶段失败，错误包含 `No module named 'a_share_radar.data_sources'`。

- [ ] **步骤 3：实现领域模型和数据源协议**

`backend/src/a_share_radar/domain/models.py`：

```python
from dataclasses import dataclass
from datetime import date, datetime
from enum import StrEnum


class Market(StrEnum):
    SH = "SH"
    SZ = "SZ"
    BJ = "BJ"


class QualityStatus(StrEnum):
    OK = "ok"
    PARTIAL = "partial"
    STALE = "stale"
    ERROR = "error"


@dataclass(frozen=True, slots=True)
class Stock:
    code: str
    market: Market
    name: str
    list_status: str = "L"
    list_date: date | None = None


@dataclass(frozen=True, slots=True)
class QuoteSnapshot:
    code: str
    market: Market
    name: str
    captured_at: datetime
    latest_price: float | None
    change_percent: float | None
    change_amount: float | None
    open_price: float | None
    high_price: float | None
    low_price: float | None
    previous_close: float | None
    volume: int | None
    amount: float | None
    turnover_rate: float | None
    total_market_cap: float | None
    source: str
    quality_status: QualityStatus


@dataclass(frozen=True, slots=True)
class Bar:
    code: str
    market: Market
    period: str
    adjustment: str
    bar_time: datetime
    open_price: float
    high_price: float
    low_price: float
    close_price: float
    volume: int
    amount: float
    source: str
    is_complete: bool = True
```

`backend/src/a_share_radar/data_sources/protocol.py`：

```python
from datetime import date, datetime
from typing import Protocol

from a_share_radar.domain.models import Bar, QuoteSnapshot, Stock


class MarketDataSource(Protocol):
    async def fetch_stock_master(self) -> list[Stock]: ...

    async def fetch_trading_days(self, start: date, end: date) -> set[date]: ...

    async def fetch_market_snapshot(self) -> list[QuoteSnapshot]: ...

    async def fetch_daily_bars(
        self, code: str, start: date, end: date, period: str, adjustment: str
    ) -> list[Bar]: ...

    async def fetch_minute_bars(
        self,
        code: str,
        start: datetime,
        end: datetime,
        period: str,
        adjustment: str,
    ) -> list[Bar]: ...
```

`backend/src/a_share_radar/data_sources/akshare_source.py` 必须实现以下核心规则：

```python
import asyncio
from datetime import date, datetime, time
from zoneinfo import ZoneInfo

import akshare as ak
import pandas as pd

from a_share_radar.domain.models import Bar, Market, QualityStatus, QuoteSnapshot, Stock

SHANGHAI = ZoneInfo("Asia/Shanghai")


def _number(value: object) -> float | None:
    if value is None or value == "-" or pd.isna(value):
        return None
    return float(value)


def _integer(value: object) -> int | None:
    number = _number(value)
    return None if number is None else int(number)


class AkshareSource:
    name = "akshare-eastmoney"

    @staticmethod
    def market_for_code(code: str) -> Market:
        if code.startswith(("4", "8", "9")):
            return Market.BJ
        if code.startswith(("5", "6", "7")):
            return Market.SH
        return Market.SZ

    @classmethod
    def normalize_snapshot(
        cls, frame: pd.DataFrame, captured_at: datetime
    ) -> list[QuoteSnapshot]:
        result: list[QuoteSnapshot] = []
        for row in frame.to_dict("records"):
            latest = _number(row.get("最新价"))
            result.append(
                QuoteSnapshot(
                    code=str(row["代码"]).zfill(6),
                    market=cls.market_for_code(str(row["代码"])),
                    name=str(row["名称"]),
                    captured_at=captured_at,
                    latest_price=latest,
                    change_percent=_number(row.get("涨跌幅")),
                    change_amount=_number(row.get("涨跌额")),
                    open_price=_number(row.get("今开")),
                    high_price=_number(row.get("最高")),
                    low_price=_number(row.get("最低")),
                    previous_close=_number(row.get("昨收")),
                    volume=_integer(row.get("成交量")),
                    amount=_number(row.get("成交额")),
                    turnover_rate=_number(row.get("换手率")),
                    total_market_cap=_number(row.get("总市值")),
                    source=cls.name,
                    quality_status=QualityStatus.OK if latest is not None else QualityStatus.PARTIAL,
                )
            )
        return result

    @classmethod
    def normalize_minute_bars(
        cls, code: str, frame: pd.DataFrame, period: str, adjustment: str
    ) -> list[Bar]:
        return [
            Bar(
                code=code,
                market=cls.market_for_code(code),
                period=period,
                adjustment=adjustment,
                bar_time=pd.Timestamp(row["时间"]).to_pydatetime().replace(tzinfo=SHANGHAI),
                open_price=float(row["开盘"]),
                high_price=float(row["最高"]),
                low_price=float(row["最低"]),
                close_price=float(row["收盘"]),
                volume=int(row["成交量"]),
                amount=float(row["成交额"]),
                source=cls.name,
                is_complete=True,
            )
            for row in frame.to_dict("records")
        ]

    async def fetch_market_snapshot(self) -> list[QuoteSnapshot]:
        frame = await asyncio.to_thread(ak.stock_zh_a_spot_em)
        return self.normalize_snapshot(frame, datetime.now(SHANGHAI))
```

同一文件增加以下完整方法；所有第三方调用都放入 `asyncio.to_thread`：

```python
    async def fetch_stock_master(self) -> list[Stock]:
        quotes = await self.fetch_market_snapshot()
        return [Stock(quote.code, quote.market, quote.name) for quote in quotes]

    async def fetch_trading_days(self, start: date, end: date) -> set[date]:
        frame = await asyncio.to_thread(ak.tool_trade_date_hist_sina)
        values = pd.to_datetime(frame["trade_date"]).dt.date
        return {value for value in values if start <= value <= end}

    @classmethod
    def normalize_history_bars(
        cls,
        code: str,
        frame: pd.DataFrame,
        period: str,
        adjustment: str,
    ) -> list[Bar]:
        return [
            Bar(
                code=code,
                market=cls.market_for_code(code),
                period=period,
                adjustment=adjustment,
                bar_time=datetime.combine(
                    pd.Timestamp(row["日期"]).date(), time(15, 0), tzinfo=SHANGHAI
                ),
                open_price=float(row["开盘"]),
                high_price=float(row["最高"]),
                low_price=float(row["最低"]),
                close_price=float(row["收盘"]),
                volume=int(row["成交量"]),
                amount=float(row["成交额"]),
                source=cls.name,
                is_complete=True,
            )
            for row in frame.to_dict("records")
        ]

    async def fetch_daily_bars(
        self,
        code: str,
        start: date,
        end: date,
        period: str,
        adjustment: str,
    ) -> list[Bar]:
        provider_period = {"1d": "daily", "1w": "weekly", "1mo": "monthly"}[period]
        provider_adjustment = "" if adjustment == "none" else adjustment
        frame = await asyncio.to_thread(
            ak.stock_zh_a_hist,
            symbol=code,
            period=provider_period,
            start_date=start.strftime("%Y%m%d"),
            end_date=end.strftime("%Y%m%d"),
            adjust=provider_adjustment,
        )
        return self.normalize_history_bars(code, frame, period, adjustment)

    async def fetch_minute_bars(
        self,
        code: str,
        start: datetime,
        end: datetime,
        period: str,
        adjustment: str,
    ) -> list[Bar]:
        provider_adjustment = "" if adjustment == "none" else adjustment
        frame = await asyncio.to_thread(
            ak.stock_zh_a_hist_min_em,
            symbol=code,
            start_date=start.strftime("%Y-%m-%d %H:%M:%S"),
            end_date=end.strftime("%Y-%m-%d %H:%M:%S"),
            period=period.removesuffix("m"),
            adjust=provider_adjustment,
        )
        return self.normalize_minute_bars(code, frame, period, adjustment)
```

- [ ] **步骤 4：运行数据源测试**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_akshare_source.py -q
.venv/bin/ruff check src tests
```

预期：`4 passed`，Ruff 通过。

- [ ] **步骤 5：提交数据源边界**

```bash
git add backend/src/a_share_radar/domain backend/src/a_share_radar/data_sources backend/tests/test_akshare_source.py
git commit -m "feat: 建立行情数据源适配层"
```

---

### Task 3｜任务 3：DuckDB 热存储与查询仓库

**文件：**

- 创建：`backend/src/a_share_radar/storage/database.py`
- 创建：`backend/src/a_share_radar/storage/repository.py`
- 创建：`backend/tests/conftest.py`
- 创建：`backend/tests/test_repository.py`

**接口：**

- 消费：`Stock`、`QuoteSnapshot`、`Bar`
- 产出：`Database(path: Path)`
- 产出：`MarketRepository.upsert_stocks(stocks)`
- 产出：`MarketRepository.save_snapshot(quotes)`
- 产出：`MarketRepository.upsert_bars(bars)`
- 产出：`MarketRepository.list_all_stocks() -> list[Stock]`
- 产出：`MarketRepository.list_stocks(query, market, sort_by, sort_order, page, page_size)`
- 产出：`MarketRepository.get_stock(market, code)`
- 产出：`MarketRepository.market_summary(stale_after_seconds, now)`
- 产出：`MarketRepository.data_status()`
- 产出：`MarketRepository.get_bars(market, code, period, start, end, adjustment)`

- [ ] **步骤 1：写幂等和查询失败测试**

`backend/tests/test_repository.py` 包含以下测试：

```python
from datetime import datetime
from zoneinfo import ZoneInfo

from a_share_radar.domain.models import Market, QualityStatus, QuoteSnapshot, Stock


def quote(price: float, captured_at: datetime) -> QuoteSnapshot:
    return QuoteSnapshot(
        code="600519", market=Market.SH, name="贵州茅台", captured_at=captured_at,
        latest_price=price, change_percent=-2.13, change_amount=-28.92,
        open_price=1350.06, high_price=1350.94, low_price=1330.04,
        previous_close=1358.98, volume=33455, amount=4472998836.0,
        turnover_rate=0.27, total_market_cap=1670000000000.0,
        source="fixture", quality_status=QualityStatus.OK,
    )


def test_save_snapshot_updates_latest_and_preserves_history(repository):
    tz = ZoneInfo("Asia/Shanghai")
    repository.upsert_stocks([Stock("600519", Market.SH, "贵州茅台")])
    repository.save_snapshot([quote(1331.0, datetime(2026, 8, 4, 10, 30, tzinfo=tz))])
    repository.save_snapshot([quote(1330.06, datetime(2026, 8, 4, 10, 31, tzinfo=tz))])

    page = repository.list_stocks(None, None, "code", "asc", 1, 50)
    assert page.total == 1
    assert page.items[0].latest_price == 1330.06
    assert repository.snapshot_count() == 2


def test_search_matches_code_prefix_and_name(repository):
    repository.upsert_stocks([
        Stock("600519", Market.SH, "贵州茅台"),
        Stock("000001", Market.SZ, "平安银行"),
    ])
    assert repository.list_stocks("600", None, "code", "asc", 1, 50).total == 1
    assert repository.list_stocks("平安", None, "code", "asc", 1, 50).total == 1
```

`backend/tests/conftest.py`：

```python
from pathlib import Path

import pytest

from a_share_radar.storage.database import Database
from a_share_radar.storage.repository import MarketRepository


@pytest.fixture
def repository(tmp_path: Path) -> MarketRepository:
    database = Database(tmp_path / "test.duckdb")
    yield MarketRepository(database)
    database.close()
```

- [ ] **步骤 2：运行测试确认存储层缺失**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_repository.py -q
```

预期：收集失败，错误包含 `No module named 'a_share_radar.storage'`。

- [ ] **步骤 3：实现数据库结构和仓库**

`Database` 初始化时执行以下表结构：

```sql
CREATE TABLE IF NOT EXISTS stock_master (
  market VARCHAR NOT NULL,
  code VARCHAR NOT NULL,
  name VARCHAR NOT NULL,
  list_status VARCHAR NOT NULL,
  list_date DATE,
  updated_at TIMESTAMPTZ NOT NULL,
  PRIMARY KEY (market, code)
);

CREATE TABLE IF NOT EXISTS latest_quote (
  market VARCHAR NOT NULL,
  code VARCHAR NOT NULL,
  captured_at TIMESTAMPTZ NOT NULL,
  latest_price DOUBLE,
  change_percent DOUBLE,
  change_amount DOUBLE,
  open_price DOUBLE,
  high_price DOUBLE,
  low_price DOUBLE,
  previous_close DOUBLE,
  volume BIGINT,
  amount DOUBLE,
  turnover_rate DOUBLE,
  total_market_cap DOUBLE,
  source VARCHAR NOT NULL,
  quality_status VARCHAR NOT NULL,
  PRIMARY KEY (market, code)
);

CREATE TABLE IF NOT EXISTS quote_snapshot_hot AS
SELECT * FROM latest_quote WHERE FALSE;

CREATE UNIQUE INDEX IF NOT EXISTS snapshot_identity
ON quote_snapshot_hot (market, code, captured_at);

CREATE TABLE IF NOT EXISTS bar_hot (
  market VARCHAR NOT NULL,
  code VARCHAR NOT NULL,
  period VARCHAR NOT NULL,
  adjustment VARCHAR NOT NULL,
  bar_time TIMESTAMPTZ NOT NULL,
  open_price DOUBLE NOT NULL,
  high_price DOUBLE NOT NULL,
  low_price DOUBLE NOT NULL,
  close_price DOUBLE NOT NULL,
  volume BIGINT NOT NULL,
  amount DOUBLE NOT NULL,
  source VARCHAR NOT NULL,
  is_complete BOOLEAN NOT NULL,
  PRIMARY KEY (market, code, period, adjustment, bar_time)
);

CREATE TABLE IF NOT EXISTS ingestion_run (
  run_id UUID PRIMARY KEY,
  kind VARCHAR NOT NULL,
  started_at TIMESTAMPTZ NOT NULL,
  finished_at TIMESTAMPTZ,
  source VARCHAR NOT NULL,
  row_count BIGINT NOT NULL DEFAULT 0,
  status VARCHAR NOT NULL,
  error_message VARCHAR
);
```

`backend/src/a_share_radar/storage/database.py` 的关键实现：

```python
from pathlib import Path
from threading import RLock

import duckdb


class Database:
    def __init__(self, path: Path):
        path.parent.mkdir(parents=True, exist_ok=True)
        self.connection = duckdb.connect(str(path))
        self.lock = RLock()
        self.initialize()

    def initialize(self) -> None:
        with self.lock:
            self.connection.execute(SCHEMA_SQL)

    def close(self) -> None:
        with self.lock:
            self.connection.close()
```

`repository.py` 定义不可变分页结果，并通过排序白名单生成 SQL：

```python
@dataclass(frozen=True, slots=True)
class StockPage:
    items: list[StockQuoteRow]
    total: int
    page: int
    page_size: int


SORT_COLUMNS = {
    "code": "s.code",
    "latest_price": "q.latest_price",
    "change_percent": "q.change_percent",
    "amount": "q.amount",
    "turnover_rate": "q.turnover_rate",
    "total_market_cap": "q.total_market_cap",
}
```

写入使用参数化 `INSERT OR REPLACE`，快照写入和 `latest_quote` 更新放在同一事务中；任何异常都回滚。

- [ ] **步骤 4：运行存储测试**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_repository.py -q
.venv/bin/ruff check src tests
```

预期：仓库测试全部通过，Ruff 通过。

- [ ] **步骤 5：提交存储层**

```bash
git add backend/src/a_share_radar/storage backend/tests/conftest.py backend/tests/test_repository.py
git commit -m "feat: 增加 DuckDB 行情存储"
```

---

### Task 4｜任务 4：全市场快照采集、校验与一分钟调度

**文件：**

- 创建：`backend/src/a_share_radar/services/market_clock.py`
- 创建：`backend/src/a_share_radar/services/snapshot_collector.py`
- 创建：`backend/src/a_share_radar/services/scheduler.py`
- 创建：`backend/tests/test_snapshot_collector.py`
- 修改：`backend/src/a_share_radar/main.py`

**接口：**

- 消费：`MarketDataSource.fetch_market_snapshot()`、`MarketRepository.save_snapshot()`
- 产出：`MarketClock.is_open(at: datetime) -> bool`
- 产出：`SnapshotCollector.collect_once(at: datetime | None = None) -> CollectionResult`
- 产出：`create_scheduler(clock, collector, archive_callback) -> AsyncIOScheduler`

- [ ] **步骤 1：写交易时段和降级失败测试**

`backend/tests/test_snapshot_collector.py`：

```python
from datetime import datetime
from zoneinfo import ZoneInfo

import pytest

from a_share_radar.services.market_clock import MarketClock
from a_share_radar.services.snapshot_collector import SnapshotCollector, SnapshotValidationError

TZ = ZoneInfo("Asia/Shanghai")


def test_market_clock_obeys_lunch_break():
    clock = MarketClock(trading_days={datetime(2026, 8, 4, tzinfo=TZ).date()})
    assert clock.is_open(datetime(2026, 8, 4, 10, 0, tzinfo=TZ)) is True
    assert clock.is_open(datetime(2026, 8, 4, 12, 0, tzinfo=TZ)) is False
    assert clock.is_open(datetime(2026, 8, 4, 14, 0, tzinfo=TZ)) is True
    assert clock.is_open(datetime(2026, 8, 8, 10, 0, tzinfo=TZ)) is False


async def test_collector_rejects_truncated_batch_without_overwriting(repository, fake_source):
    fake_source.snapshot_rows = fake_source.snapshot_rows[:1]
    collector = SnapshotCollector(fake_source, repository, minimum_expected_count=2)

    with pytest.raises(SnapshotValidationError):
        await collector.collect_once(datetime(2026, 8, 4, 10, 31, tzinfo=TZ))

    assert repository.snapshot_count() == 0


async def test_collector_retries_transient_source_errors(repository, fake_source):
    fake_source.snapshot_failures = 2
    collector = SnapshotCollector(
        fake_source,
        repository,
        minimum_expected_count=2,
        retry_delays=(0, 0),
    )

    result = await collector.collect_once(datetime(2026, 8, 4, 10, 31, tzinfo=TZ))

    assert result.row_count == 2
    assert fake_source.snapshot_requests == 3
```

在 `conftest.py` 增加 `FakeSource`。它包含两只股票、两条快照和固定 K 线，并记录历史请求；所有方法只返回内存数据，不访问公网：

```python
from dataclasses import dataclass
from datetime import date, datetime
from zoneinfo import ZoneInfo

import pytest

from a_share_radar.domain.models import Bar, Market, QualityStatus, QuoteSnapshot, Stock

TZ = ZoneInfo("Asia/Shanghai")


@dataclass(frozen=True)
class DailyRequest:
    code: str
    start: date
    end: date
    period: str
    adjustment: str


@dataclass(frozen=True)
class MinuteRequest:
    code: str
    start: datetime
    end: datetime
    period: str
    adjustment: str


class FakeSource:
    def __init__(self):
        captured = datetime(2026, 8, 4, 10, 31, tzinfo=TZ)
        self.stock_rows = [
            Stock("600519", Market.SH, "贵州茅台"),
            Stock("000001", Market.SZ, "平安银行"),
        ]
        self.snapshot_rows = [
            QuoteSnapshot(
                "600519", Market.SH, "贵州茅台", captured, 1330.06, -2.13, -28.92,
                1350.06, 1350.94, 1330.04, 1358.98, 33455, 4472998836.0,
                0.27, 1670000000000.0, "fixture", QualityStatus.OK,
            ),
            QuoteSnapshot(
                "000001", Market.SZ, "平安银行", captured, 12.50, 0.40, 0.05,
                12.45, 12.56, 12.40, 12.45, 100000, 125000000.0,
                0.50, 242000000000.0, "fixture", QualityStatus.OK,
            ),
        ]
        self.bar_rows = [
            Bar(
                "600519", Market.SH, "1m", "none", captured, 1334.20, 1335.08,
                1330.04, 1330.06, 1820, 24250000.0, "fixture", True,
            ),
            Bar(
                "600519", Market.SH, "1d", "qfq",
                datetime(2026, 8, 4, 15, 0, tzinfo=TZ), 1350.06, 1350.94,
                1330.04, 1330.06, 33455, 4472998836.0, "fixture", True,
            ),
        ]
        self.daily_requests: list[DailyRequest] = []
        self.minute_requests: list[MinuteRequest] = []
        self.snapshot_requests = 0
        self.snapshot_failures = 0
        self.minute_error: Exception | None = None

    async def fetch_stock_master(self):
        return list(self.stock_rows)

    async def fetch_trading_days(self, start, end):
        return {date(2026, 8, 4)}

    async def fetch_market_snapshot(self):
        self.snapshot_requests += 1
        if self.snapshot_failures > 0:
            self.snapshot_failures -= 1
            raise RuntimeError("模拟上游瞬时失败")
        return list(self.snapshot_rows)

    async def fetch_daily_bars(self, code, start, end, period, adjustment):
        self.daily_requests.append(DailyRequest(code, start, end, period, adjustment))
        return [bar for bar in self.bar_rows if bar.code == code and bar.period == period]

    async def fetch_minute_bars(self, code, start, end, period, adjustment):
        self.minute_requests.append(MinuteRequest(code, start, end, period, adjustment))
        if self.minute_error is not None:
            raise self.minute_error
        return [bar for bar in self.bar_rows if bar.code == code and bar.period == period]


@pytest.fixture
def fake_source():
    return FakeSource()
```

- [ ] **步骤 2：运行测试确认服务缺失**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_snapshot_collector.py -q
```

预期：收集失败，错误包含 `No module named 'a_share_radar.services'`。

- [ ] **步骤 3：实现交易时钟、采集器和调度器**

`market_clock.py`：

```python
from datetime import date, datetime, time


class MarketClock:
    def __init__(self, trading_days: set[date]):
        self.trading_days = trading_days

    def is_open(self, at: datetime) -> bool:
        if at.date() not in self.trading_days:
            return False
        current = at.timetz().replace(tzinfo=None)
        return time(9, 30) <= current <= time(11, 30) or time(13, 0) <= current <= time(15, 0)
```

`snapshot_collector.py`：

```python
import asyncio
from dataclasses import dataclass
from datetime import datetime

from a_share_radar.data_sources.protocol import MarketDataSource
from a_share_radar.storage.repository import MarketRepository


class SnapshotValidationError(RuntimeError):
    pass


@dataclass(frozen=True, slots=True)
class CollectionResult:
    captured_at: datetime
    row_count: int
    source: str


class SnapshotCollector:
    def __init__(
        self,
        source: MarketDataSource,
        repository: MarketRepository,
        minimum_expected_count: int = 4000,
        retry_delays: tuple[float, ...] = (1.0, 2.0),
    ):
        self.source = source
        self.repository = repository
        self.minimum_expected_count = minimum_expected_count
        self.retry_delays = retry_delays

    async def _fetch_quotes(self):
        for attempt in range(len(self.retry_delays) + 1):
            try:
                return await self.source.fetch_market_snapshot()
            except Exception:
                if attempt == len(self.retry_delays):
                    raise
                await asyncio.sleep(self.retry_delays[attempt])
        raise AssertionError("重试循环不应运行到此处")

    async def collect_once(self, at: datetime | None = None) -> CollectionResult:
        quotes = await self._fetch_quotes()
        identities = {(quote.market, quote.code) for quote in quotes}
        if len(quotes) < self.minimum_expected_count:
            raise SnapshotValidationError(f"行情数量异常：仅取得 {len(quotes)} 条")
        if len(identities) != len(quotes):
            raise SnapshotValidationError("行情批次包含重复股票代码")
        self.repository.save_snapshot(quotes)
        return CollectionResult(quotes[0].captured_at, len(quotes), quotes[0].source)
```

`scheduler.py`：

```python
import logging
from collections.abc import Awaitable, Callable
from datetime import datetime
from zoneinfo import ZoneInfo

from apscheduler.schedulers.asyncio import AsyncIOScheduler
from apscheduler.triggers.cron import CronTrigger

logger = logging.getLogger(__name__)
SHANGHAI = ZoneInfo("Asia/Shanghai")


def create_scheduler(
    clock: MarketClock,
    collector: SnapshotCollector,
    archive_callback: Callable[[], Awaitable[None]],
) -> AsyncIOScheduler:
    scheduler = AsyncIOScheduler(timezone=SHANGHAI)

    async def collect_if_open() -> None:
        now = datetime.now(SHANGHAI)
        if not clock.is_open(now):
            return
        try:
            await collector.collect_once(now)
        except Exception:
            logger.exception("全市场行情采集失败，继续保留上一批有效数据")

    scheduler.add_job(
        collect_if_open,
        CronTrigger(minute="*", second=5, timezone=SHANGHAI),
        max_instances=1,
        coalesce=True,
    )
    scheduler.add_job(
        archive_callback,
        CronTrigger(hour=15, minute=10, day_of_week="mon-fri", timezone=SHANGHAI),
        max_instances=1,
        coalesce=True,
    )
    return scheduler
```

`main.py` 使用 FastAPI `lifespan`，测试可以注入数据源和数据库：

```python
from contextlib import asynccontextmanager
from datetime import date, datetime, timedelta
from zoneinfo import ZoneInfo

from fastapi import FastAPI


def create_app(settings=None, source=None, database=None) -> FastAPI:
    resolved_settings = settings or Settings()
    resolved_database = database or Database(resolved_settings.database_path)
    resolved_source = source or AkshareSource()
    repository = MarketRepository(resolved_database)

    @asynccontextmanager
    async def lifespan(app: FastAPI):
        stocks = await resolved_source.fetch_stock_master()
        repository.upsert_stocks(stocks)
        today = date.today()
        trading_days = await resolved_source.fetch_trading_days(
            today - timedelta(days=370), today + timedelta(days=370)
        )
        clock = MarketClock(trading_days)
        minimum_expected_count = 1 if source is not None or resolved_settings.fixture_source else 4000
        collector = SnapshotCollector(
            resolved_source, repository, minimum_expected_count=minimum_expected_count
        )
        await collector.collect_once(datetime.now(ZoneInfo("Asia/Shanghai")))

        async def archive_later() -> None:
            return None

        scheduler = create_scheduler(clock, collector, archive_later)
        app.state.repository = repository
        app.state.source = resolved_source
        app.state.clock = clock
        app.state.collector = collector
        scheduler.start()
        try:
            yield
        finally:
            scheduler.shutdown(wait=False)
            if database is None:
                resolved_database.close()

    app = FastAPI(title="A 股雷达", version="0.1.0", lifespan=lifespan)
    app.state.settings = resolved_settings
    app.state.repository = repository
    app.state.source = resolved_source
    return app
```

任务 5 实现归档后，把 `archive_later` 的空实现替换为当天归档调用。任务 10 的固定数据源模式会禁用公网采集调度器。

- [ ] **步骤 4：运行采集测试**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_snapshot_collector.py tests/test_repository.py -q
.venv/bin/ruff check src tests
```

预期：全部通过；截断批次不会写入数据库。

- [ ] **步骤 5：提交采集调度**

```bash
git add backend/src/a_share_radar/services backend/src/a_share_radar/main.py backend/tests
git commit -m "feat: 增加一分钟行情采集调度"
```

---

### Task 5｜任务 5：历史日 K、按需分钟 K 与 Parquet 归档

**文件：**

- 创建：`backend/src/a_share_radar/services/bar_service.py`
- 创建：`backend/src/a_share_radar/storage/archive.py`
- 创建：`backend/tests/test_bar_service.py`
- 创建：`backend/tests/test_archive.py`
- 修改：`backend/src/a_share_radar/storage/repository.py`
- 修改：`backend/src/a_share_radar/services/scheduler.py`
- 修改：`backend/src/a_share_radar/main.py`

**接口：**

- 产出：`BarService.ensure_daily_history(stock, end_date) -> list[Bar]`
- 产出：`BarService.get_bars(market, code, period, range_name, adjustment, now) -> list[Bar]`
- 产出：`HistoryBootstrapper.run() -> None`
- 产出：`archive_snapshots(repository, trade_date, data_dir) -> Path`

- [ ] **步骤 1：写 60 日回补、今日分钟和归档失败测试**

`backend/tests/test_bar_service.py`：

```python
from datetime import date, datetime
from zoneinfo import ZoneInfo

from a_share_radar.domain.models import Market, Stock
from a_share_radar.services.bar_service import BarService


async def test_daily_history_requests_sixty_trading_days(repository, fake_source):
    service = BarService(fake_source, repository, history_days=60)
    await service.ensure_daily_history(
        Stock("600519", Market.SH, "贵州茅台"), date(2026, 8, 4)
    )
    request = fake_source.daily_requests[0]
    assert request.code == "600519"
    assert request.end == date(2026, 8, 4)
    assert (request.end - request.start).days >= 84


async def test_today_uses_native_one_minute_source(repository, fake_source):
    service = BarService(fake_source, repository, history_days=60)
    now = datetime(2026, 8, 4, 10, 31, tzinfo=ZoneInfo("Asia/Shanghai"))
    bars = await service.get_bars(Market.SH, "600519", "1m", "today", "none", now)
    assert fake_source.minute_requests[0].period == "1m"
    assert all(bar.period == "1m" for bar in bars)


async def test_minute_source_failure_returns_saved_bars(repository, fake_source):
    repository.upsert_bars([fake_source.bar_rows[0]])
    fake_source.minute_error = RuntimeError("模拟分钟接口不可用")
    service = BarService(fake_source, repository, history_days=60)
    now = datetime(2026, 8, 4, 10, 31, tzinfo=ZoneInfo("Asia/Shanghai"))

    bars = await service.get_bars(Market.SH, "600519", "1m", "today", "none", now)

    assert bars == [fake_source.bar_rows[0]]
```

`backend/tests/test_archive.py`：

```python
from datetime import date

import duckdb

from a_share_radar.storage.archive import archive_snapshots


def test_archive_writes_verified_parquet_and_removes_hot_rows(
    repository, tmp_path, fake_source
):
    repository.save_snapshot(fake_source.snapshot_rows[:1])
    output = archive_snapshots(repository, date(2026, 8, 4), tmp_path)
    archived = duckdb.sql(f"SELECT count(*) FROM read_parquet('{output}')").fetchone()[0]
    assert archived == 1
    assert repository.snapshot_count_for_date(date(2026, 8, 4)) == 0
```

- [ ] **步骤 2：运行测试确认服务与归档缺失**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_bar_service.py tests/test_archive.py -q
```

预期：收集失败，缺少 `bar_service` 和 `archive`。

- [ ] **步骤 3：实现历史服务和原子归档**

`bar_service.py` 采用以下范围映射：

```python
RANGE_DAYS = {
    "5d": 7,
    "60d": 90,
    "6mo": 190,
    "ytd": 370,
    "1y": 370,
    "5y": 1830,
}

PERIODS = {"1m", "5m", "15m", "30m", "60m", "1d", "1w", "1mo"}
ADJUSTMENTS = {"none", "qfq", "hfq"}
```

`BarService` 的校验、时间范围和读取行为实现为：

```python
def _validate(self, period: str, range_name: str, adjustment: str) -> None:
    if period not in PERIODS:
        raise ValueError(f"不支持的 K 线周期：{period}")
    if adjustment not in ADJUSTMENTS:
        raise ValueError(f"不支持的复权方式：{adjustment}")
    if range_name not in {"all", "today", *RANGE_DAYS}:
        raise ValueError(f"不支持的时间范围：{range_name}")
    if range_name == "today" and period != "1m":
        raise ValueError("今日视图只允许一分钟 K")
    if period == "1m" and adjustment != "none":
        raise ValueError("免费一分钟 K 只允许不复权")


def _range(self, now: datetime, range_name: str) -> tuple[datetime, datetime]:
    if range_name == "today":
        return datetime.combine(now.date(), time(9, 30), tzinfo=now.tzinfo), now
    if range_name == "ytd":
        return datetime(now.year, 1, 1, tzinfo=now.tzinfo), now
    if range_name == "all":
        return datetime(1990, 1, 1, tzinfo=now.tzinfo), now
    return now - timedelta(days=RANGE_DAYS[range_name]), now


async def ensure_daily_history(self, stock: Stock, end_date: date) -> list[Bar]:
    start_date = end_date - timedelta(days=100)
    fetched = await self.source.fetch_daily_bars(
        stock.code, start_date, end_date, "1d", "qfq"
    )
    selected = fetched[-self.history_days :]
    self.repository.upsert_bars(selected)
    return selected


async def get_bars(self, market, code, period, range_name, adjustment, now):
    self._validate(period, range_name, adjustment)
    start, end = self._range(now, range_name)
    key = (market, code, period, range_name, adjustment)
    async with self._locks[key]:
        cached = self.repository.get_bars(market, code, period, start, end, adjustment)
        if period.endswith("m"):
            try:
                fetched = await self.source.fetch_minute_bars(
                    code, start, end, period, adjustment
                )
            except Exception:
                if cached:
                    return cached
                raise
        elif not cached:
            fetched = await self.source.fetch_daily_bars(
                code, start.date(), end.date(), period, adjustment
            )
        else:
            fetched = []
        self.repository.upsert_bars(fetched)
        return self.repository.get_bars(market, code, period, start, end, adjustment)
```

`BarService.__init__` 创建 `self._locks = defaultdict(asyncio.Lock)`；文件顶部导入 `asyncio`、`defaultdict`、`date`、`datetime`、`time`、`timedelta` 和 `ZoneInfo`。分钟查询每次页面轮询都允许访问适配器，但同一股票、周期和范围的并发请求由同一把锁串行化；分钟接口失败且已有本地记录时返回缓存，日、周、月 K 在本地已有数据时直接读取。

同一文件增加后台日 K 回补器：

```python
import logging

logger = logging.getLogger(__name__)


class HistoryBootstrapper:
    def __init__(
        self,
        bar_service: BarService,
        repository: MarketRepository,
        delay_seconds: float,
    ):
        self.bar_service = bar_service
        self.repository = repository
        self.delay_seconds = delay_seconds

    async def run(self) -> None:
        end_date = datetime.now(ZoneInfo("Asia/Shanghai")).date()
        for stock in self.repository.list_all_stocks():
            try:
                await self.bar_service.ensure_daily_history(stock, end_date)
            except asyncio.CancelledError:
                raise
            except Exception:
                logger.exception("日 K 回补失败：%s.%s", stock.market, stock.code)
            await asyncio.sleep(self.delay_seconds)
```

`main.py` 在创建 `repository` 后立即创建 `BarService` 并保存到 `app.state.bar_service`，使不启动生命周期的 HTTP 单元测试也能查询本地数据。股票主数据写入后创建 `HistoryBootstrapper`，用 `asyncio.create_task` 启动回补；应用关闭时取消并等待任务。把任务 4 的空归档回调替换为：

```python
async def archive_later() -> None:
    trade_date = datetime.now(SHANGHAI).date()
    await asyncio.to_thread(
        archive_snapshots, repository, trade_date, resolved_settings.data_dir
    )
```

对于 `range_name="today"`，起点固定为当天 09:30，周期强制为 `1m`。若上游未明确提供未完成柱，所有返回柱的 `is_complete` 保持 `True`，不得自行制造动态柱。

`archive.py`：

```python
from datetime import date
from pathlib import Path


def archive_snapshots(repository, trade_date: date, data_dir: Path) -> Path:
    target_dir = data_dir / "snapshots" / f"trade_date={trade_date.isoformat()}"
    target_dir.mkdir(parents=True, exist_ok=True)
    target = target_dir / "part-000.parquet"
    temporary = target.with_suffix(".parquet.tmp")
    repository.copy_snapshots_to_parquet(trade_date, temporary)
    expected = repository.snapshot_count_for_date(trade_date)
    actual = repository.parquet_count(temporary)
    if expected != actual or actual == 0:
        temporary.unlink(missing_ok=True)
        raise RuntimeError(f"归档校验失败：热数据 {expected} 条，文件 {actual} 条")
    temporary.replace(target)
    repository.delete_snapshots_for_date(trade_date)
    return target
```

删除热数据必须发生在临时文件校验和原子替换成功之后。

- [ ] **步骤 4：运行 K 线和归档测试**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_bar_service.py tests/test_archive.py -q
.venv/bin/ruff check src tests
```

预期：全部通过；归档文件行数与删除前热数据一致。

- [ ] **步骤 5：提交历史行情能力**

```bash
git add backend/src/a_share_radar/services/bar_service.py backend/src/a_share_radar/storage backend/tests
git commit -m "feat: 增加历史 K 线与行情归档"
```

---

### Task 6｜任务 6：市场、股票、K 线和数据状态 HTTP API

**文件：**

- 创建：`backend/src/a_share_radar/api/schemas.py`
- 创建：`backend/src/a_share_radar/api/routes.py`
- 创建：`backend/tests/test_api.py`
- 修改：`backend/src/a_share_radar/main.py`

**接口：**

- 产出：`GET /api/market/summary`
- 产出：`GET /api/market/stocks`
- 产出：`GET /api/stocks/{market}/{code}`
- 产出：`GET /api/stocks/{market}/{code}/bars`
- 产出：`GET /api/system/data-status`

- [ ] **步骤 1：写 HTTP 合约失败测试**

`backend/tests/test_api.py`：

```python
from httpx import ASGITransport, AsyncClient

from a_share_radar.main import create_app


async def test_stock_list_search_and_pagination(app_with_fixture_data):
    async with AsyncClient(
        transport=ASGITransport(app=app_with_fixture_data), base_url="http://test"
    ) as client:
        response = await client.get("/api/market/stocks", params={"query": "茅台"})

    assert response.status_code == 200
    body = response.json()
    assert body["total"] == 1
    assert body["items"][0]["code"] == "600519"
    assert body["items"][0]["market"] == "SH"


async def test_today_bars_return_one_minute_semantics(app_with_fixture_data):
    async with AsyncClient(
        transport=ASGITransport(app=app_with_fixture_data), base_url="http://test"
    ) as client:
        response = await client.get(
            "/api/stocks/SH/600519/bars",
            params={"period": "1m", "range": "today", "adjustment": "none"},
        )

    assert response.status_code == 200
    assert response.json()["period"] == "1m"
    assert response.json()["range"] == "today"
    assert response.json()["items"][0]["bar_time"].endswith("+08:00")


async def test_invalid_sort_field_returns_422(app_with_fixture_data):
    async with AsyncClient(
        transport=ASGITransport(app=app_with_fixture_data), base_url="http://test"
    ) as client:
        response = await client.get("/api/market/stocks", params={"sort_by": "drop table"})
    assert response.status_code == 422
```

在 `backend/tests/conftest.py` 增加应用夹具，使用任务 3 的 `quote` 固定数据和任务 4 的 `fake_source`：

```python
@pytest.fixture
def app_with_fixture_data(tmp_path, fake_source):
    settings = Settings(data_dir=tmp_path, fixture_source=True)
    database = Database(settings.database_path)
    repository = MarketRepository(database)
    repository.upsert_stocks(fake_source.stock_rows)
    repository.save_snapshot(fake_source.snapshot_rows)
    repository.upsert_bars(fake_source.bar_rows)
    app = create_app(settings=settings, source=fake_source, database=database)
    yield app
    database.close()
```

- [ ] **步骤 2：运行 API 测试确认接口缺失**

运行：

```bash
cd backend
.venv/bin/pytest tests/test_api.py -q
```

预期：接口返回 404 或缺少 `api` 模块。

- [ ] **步骤 3：实现 Pydantic 响应和路由**

`schemas.py` 定义以下模型：

```python
class MarketSummaryResponse(BaseModel):
    total: int
    rising: int
    falling: int
    flat: int
    amount: float
    market_status: Literal["open", "closed"]
    last_updated_at: datetime | None
    stale: bool


class StockQuoteResponse(BaseModel):
    market: Market
    code: str
    name: str
    latest_price: float | None
    change_percent: float | None
    change_amount: float | None
    open_price: float | None
    high_price: float | None
    low_price: float | None
    previous_close: float | None
    volume: int | None
    amount: float | None
    turnover_rate: float | None
    total_market_cap: float | None
    captured_at: datetime | None
    quality_status: QualityStatus | None


class StockPageResponse(BaseModel):
    items: list[StockQuoteResponse]
    total: int
    page: int
    page_size: int


class BarResponse(BaseModel):
    bar_time: datetime
    open_price: float
    high_price: float
    low_price: float
    close_price: float
    volume: int
    amount: float
    is_complete: bool


class BarSeriesResponse(BaseModel):
    market: Market
    code: str
    period: str
    range: str
    adjustment: str
    source: str | None
    last_updated_at: datetime | None
    items: list[BarResponse]
```

`routes.py` 使用 `APIRouter(prefix="/api")`。`sort_by` 使用 `Literal["code", "latest_price", "change_percent", "amount", "turnover_rate", "total_market_cap"]`；`sort_order` 使用 `Literal["asc", "desc"]`；`page_size` 范围为 10–200。端点实现为：

```python
from datetime import datetime
from typing import Literal
from zoneinfo import ZoneInfo

from fastapi import APIRouter, HTTPException, Query, Request

router = APIRouter(prefix="/api")
SHANGHAI = ZoneInfo("Asia/Shanghai")
SortField = Literal[
    "code", "latest_price", "change_percent", "amount", "turnover_rate",
    "total_market_cap",
]


@router.get("/market/summary", response_model=MarketSummaryResponse)
def market_summary(request: Request):
    settings = request.app.state.settings
    return request.app.state.repository.market_summary(
        settings.stale_after_seconds, datetime.now(SHANGHAI)
    )


@router.get("/market/stocks", response_model=StockPageResponse)
def stock_list(
    request: Request,
    query: str | None = None,
    market: Market | None = None,
    sort_by: SortField = "code",
    sort_order: Literal["asc", "desc"] = "asc",
    page: int = Query(1, ge=1),
    page_size: int = Query(50, ge=10, le=200),
):
    return request.app.state.repository.list_stocks(
        query, market, sort_by, sort_order, page, page_size
    )


@router.get("/stocks/{market}/{code}", response_model=StockQuoteResponse)
def stock_detail(request: Request, market: Market, code: str):
    stock = request.app.state.repository.get_stock(market, code)
    if stock is None:
        raise HTTPException(status_code=404, detail="未找到该股票")
    return stock


@router.get("/stocks/{market}/{code}/bars", response_model=BarSeriesResponse)
async def stock_bars(
    request: Request,
    market: Market,
    code: str,
    period: Literal["1m", "5m", "15m", "30m", "60m", "1d", "1w", "1mo"],
    range: Literal["today", "5d", "60d", "6mo", "ytd", "1y", "5y", "all"],
    adjustment: Literal["none", "qfq", "hfq"] = "qfq",
):
    bars = await request.app.state.bar_service.get_bars(
        market, code, period, range, adjustment, datetime.now(SHANGHAI)
    )
    return BarSeriesResponse(
        market=market,
        code=code,
        period=period,
        range=range,
        adjustment=adjustment,
        source=bars[-1].source if bars else None,
        last_updated_at=bars[-1].bar_time if bars else None,
        items=[BarResponse.model_validate(bar, from_attributes=True) for bar in bars],
    )


@router.get("/system/data-status")
def data_status(request: Request):
    return request.app.state.repository.data_status()
```

`main.py` 在应用工厂中挂载路由，并将 `repository`、`bar_service`、`clock` 放入 `app.state`。测试工厂必须能够注入临时数据库和固定数据源。为本地前端加入精确 CORS 配置：

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://127.0.0.1:5173",
        "http://localhost:5173",
        "http://127.0.0.1:4173",
    ],
    allow_credentials=False,
    allow_methods=["GET"],
    allow_headers=["Content-Type"],
)
```

- [ ] **步骤 4：运行完整后端测试**

运行：

```bash
cd backend
.venv/bin/pytest -q
.venv/bin/ruff check src tests
```

预期：全部后端测试通过，Ruff 通过。

- [ ] **步骤 5：提交 HTTP API**

```bash
git add backend/src/a_share_radar/api backend/src/a_share_radar/main.py backend/tests
git commit -m "feat: 提供行情与 K 线接口"
```

---

### Task 7｜任务 7：前端项目骨架与单菜单应用壳

**文件：**

- 创建：`frontend/package.json`
- 创建：`frontend/tsconfig.json`
- 创建：`frontend/vite.config.ts`
- 创建：`frontend/index.html`
- 创建：`frontend/src/main.tsx`
- 创建：`frontend/src/app/App.tsx`
- 创建：`frontend/src/app/styles.css`
- 创建：`frontend/src/components/AppShell.tsx`
- 创建：`frontend/src/test/setup.ts`
- 创建：`frontend/src/components/AppShell.test.tsx`

**接口：**

- 产出：`AppShell({ children }: PropsWithChildren)`
- 产出：`/` 股票列表路由和 `/stocks/:market/:code` 详情基础页面

- [ ] **步骤 1：创建前端配置和失败测试**

`frontend/package.json`：

```json
{
  "name": "a-share-radar-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test"
  },
  "dependencies": {
    "echarts": "6.1.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "react-router-dom": "7.18.2"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@testing-library/jest-dom": "7.0.0",
    "@testing-library/react": "16.3.2",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "6.0.5",
    "jsdom": "30.0.1",
    "typescript": "7.0.2",
    "vite": "8.2.0",
    "vitest": "4.1.10"
  }
}
```

`frontend/tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "types": ["vitest/globals", "@testing-library/jest-dom"]
  },
  "include": ["src", "vite.config.ts", "playwright.config.ts"]
}
```

`frontend/vite.config.ts`：

```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./src/test/setup.ts"],
  },
});
```

`frontend/index.html`：

```html
<!doctype html>
<html lang="zh-CN">
  <head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>A 股雷达</title></head>
  <body><div id="root"></div><script type="module" src="/src/main.tsx"></script></body>
</html>
```

`frontend/src/test/setup.ts`：

```ts
import "@testing-library/jest-dom/vitest";
```

`AppShell.test.tsx`：

```tsx
import { render, screen } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import { AppShell } from "./AppShell";

test("左侧只显示全部股票一个菜单项", () => {
  render(
    <MemoryRouter>
      <AppShell><div>内容</div></AppShell>
    </MemoryRouter>,
  );
  expect(screen.getByRole("link", { name: "全部股票" })).toBeInTheDocument();
  expect(screen.queryByText("条件筛选")).not.toBeInTheDocument();
  expect(screen.queryByText("我的关注")).not.toBeInTheDocument();
});
```

- [ ] **步骤 2：安装依赖并确认组件测试失败**

运行：

```bash
cd frontend
pnpm install
pnpm test -- AppShell.test.tsx
```

预期：失败，错误包含 `Failed to resolve import "./AppShell"`。

- [ ] **步骤 3：实现 HeroUI 浅色应用壳**

`AppShell.tsx`：

```tsx
import type { PropsWithChildren } from "react";
import { NavLink } from "react-router-dom";

export function AppShell({ children }: PropsWithChildren) {
  return (
    <div className="app-shell">
      <aside className="sidebar">
        <div className="brand"><span className="brand-mark">A</span>A 股雷达</div>
        <div className="nav-label">市场</div>
        <NavLink className="nav-item" to="/">▦<span>全部股票</span></NavLink>
        <div className="source-status">
          <span className="live-dot" />行情服务状态由后端提供
        </div>
      </aside>
      <main className="workspace">{children}</main>
    </div>
  );
}
```

`styles.css` 先定义以下视觉变量和应用框架；列表与详情专用样式分别在任务 8 和任务 9 中加入：

```css
:root {
  font-family: Inter, "PingFang SC", "Microsoft YaHei", sans-serif;
  color: #20263a;
  background: #f6f8fb;
  --primary: #5b69ec;
  --up: #e5484d;
  --down: #16a36f;
  --line: #e9edf3;
  --muted: #8a93a6;
}
* { box-sizing: border-box; }
body { margin: 0; min-width: 1024px; background: #f6f8fb; }
.app-shell { min-height: 100vh; display: grid; grid-template-columns: 176px minmax(0, 1fr); }
.sidebar { position: sticky; top: 0; height: 100vh; padding: 20px 14px; background: #fff; border-right: 1px solid var(--line); }
.brand { display: flex; align-items: center; gap: 10px; padding: 0 8px 22px; font-weight: 800; }
.brand-mark { width: 32px; height: 32px; display: grid; place-items: center; border-radius: 10px; color: #fff; background: linear-gradient(135deg, #6775f6, #4c5adb); }
.nav-item { display: flex; align-items: center; gap: 10px; height: 38px; padding: 0 11px; border-radius: 10px; color: #5360dc; background: #eef0ff; font-weight: 700; text-decoration: none; }
.workspace { min-width: 0; }
.up { color: var(--up); }
.down { color: var(--down); }
```

`frontend/src/app/App.tsx`：

```tsx
import { BrowserRouter, Route, Routes } from "react-router-dom";
import { AppShell } from "../components/AppShell";

function StockListBasePage() { return <h1>全市场行情</h1>; }
function StockDetailBasePage() { return <h1>个股详情</h1>; }

export function App() {
  return (
    <BrowserRouter>
      <AppShell>
        <Routes>
          <Route path="/" element={<StockListBasePage />} />
          <Route path="/stocks/:market/:code" element={<StockDetailBasePage />} />
        </Routes>
      </AppShell>
    </BrowserRouter>
  );
}
```

`frontend/src/main.tsx`：

```tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { App } from "./app/App";
import "./app/styles.css";

createRoot(document.getElementById("root")!).render(
  <StrictMode><App /></StrictMode>,
);
```

- [ ] **步骤 4：运行前端测试和构建**

运行：

```bash
cd frontend
pnpm test
pnpm build
```

预期：测试通过，Vite 构建成功并生成 `dist/`。

- [ ] **步骤 5：提交前端骨架**

```bash
git add frontend
git commit -m "feat: 建立浅色行情前端骨架"
```

---

### Task 8｜任务 8：全市场股票列表、搜索、分页和一分钟轮询

**文件：**

- 创建：`frontend/src/api/types.ts`
- 创建：`frontend/src/api/client.ts`
- 创建：`frontend/src/hooks/usePolling.ts`
- 创建：`frontend/src/components/MarketSummary.tsx`
- 创建：`frontend/src/components/StockTable.tsx`
- 创建：`frontend/src/pages/StockListPage.tsx`
- 创建：`frontend/src/test/fixtures.ts`
- 创建：`frontend/src/pages/StockListPage.test.tsx`
- 修改：`frontend/src/app/App.tsx`
- 修改：`frontend/src/app/styles.css`

**接口：**

- 消费：`GET /api/market/summary`、`GET /api/market/stocks`
- 产出：`marketApi.getSummary()`、`marketApi.getStocks(params)`
- 产出：`usePolling(load, 60_000)`
- 产出：`StockListPage`

- [ ] **步骤 1：写列表和搜索失败测试**

`StockListPage.test.tsx`：

```tsx
import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import { marketApi } from "../api/client";
import { summaryFixture, stockPageFixture } from "../test/fixtures";
import { StockListPage } from "./StockListPage";

vi.mock("../api/client", () => ({
  marketApi: { getSummary: vi.fn(), getStocks: vi.fn() },
}));

test("一只股票只占一行并可按名称搜索", async () => {
  vi.mocked(marketApi.getSummary).mockResolvedValue(summaryFixture);
  vi.mocked(marketApi.getStocks).mockResolvedValue(stockPageFixture);
  render(<MemoryRouter><StockListPage /></MemoryRouter>);

  expect(await screen.findByRole("row", { name: /贵州茅台/ })).toBeInTheDocument();
  fireEvent.change(screen.getByPlaceholderText("搜索股票代码或名称"), {
    target: { value: "茅台" },
  });
  await waitFor(() => expect(marketApi.getStocks).toHaveBeenLastCalledWith(
    expect.objectContaining({ query: "茅台" }),
  ));
});


test("数据过期时显示最后更新时间", async () => {
  vi.mocked(marketApi.getSummary).mockResolvedValue({ ...summaryFixture, stale: true });
  vi.mocked(marketApi.getStocks).mockResolvedValue(stockPageFixture);
  render(<MemoryRouter><StockListPage /></MemoryRouter>);
  expect(await screen.findByText("数据可能已过期")).toBeInTheDocument();
});
```

- [ ] **步骤 2：运行测试确认列表页缺失**

运行：

```bash
cd frontend
pnpm test -- StockListPage.test.tsx
```

预期：失败，无法解析 `StockListPage`。

- [ ] **步骤 3：实现类型化客户端、轮询和列表页**

`types.ts` 精确镜像后端字段；价格字段使用 `number | null`，时间使用 ISO 字符串：

```ts
export type Market = "SH" | "SZ" | "BJ";
export type QualityStatus = "ok" | "partial" | "stale" | "error";

export interface MarketSummary {
  total: number;
  rising: number;
  falling: number;
  flat: number;
  amount: number;
  market_status: "open" | "closed";
  last_updated_at: string | null;
  stale: boolean;
}

export interface StockQuote {
  market: Market;
  code: string;
  name: string;
  latest_price: number | null;
  change_percent: number | null;
  change_amount: number | null;
  open_price: number | null;
  high_price: number | null;
  low_price: number | null;
  previous_close: number | null;
  volume: number | null;
  amount: number | null;
  turnover_rate: number | null;
  total_market_cap: number | null;
  captured_at: string | null;
  quality_status: QualityStatus | null;
}

export interface StockPage {
  items: StockQuote[];
  total: number;
  page: number;
  page_size: number;
}

export interface StockQuery {
  query?: string;
  market?: Market;
  page: number;
  pageSize: number;
  sortBy: "code" | "latest_price" | "change_percent" | "amount" | "turnover_rate" | "total_market_cap";
  sortOrder: "asc" | "desc";
}

export interface Bar {
  bar_time: string;
  open_price: number;
  high_price: number;
  low_price: number;
  close_price: number;
  volume: number;
  amount: number;
  is_complete: boolean;
}

export interface BarSeries {
  market: Market;
  code: string;
  period: string;
  range: string;
  adjustment: string;
  source: string | null;
  last_updated_at: string | null;
  items: Bar[];
}
```

`client.ts`：

```ts
import type { MarketSummary, StockPage, StockQuery } from "./types";

const API_BASE = import.meta.env.VITE_API_BASE_URL ?? "http://127.0.0.1:8000";

async function request<T>(path: string): Promise<T> {
  const response = await fetch(`${API_BASE}${path}`);
  if (!response.ok) throw new Error(`行情接口失败：${response.status}`);
  return response.json() as Promise<T>;
}

export const marketApi = {
  getSummary: () => request<MarketSummary>("/api/market/summary"),
  getStocks: (params: StockQuery) => {
    const query = new URLSearchParams();
    if (params.query) query.set("query", params.query);
    if (params.market) query.set("market", params.market);
    query.set("page", String(params.page));
    query.set("page_size", String(params.pageSize));
    query.set("sort_by", params.sortBy);
    query.set("sort_order", params.sortOrder);
    return request<StockPage>(`/api/market/stocks?${query}`);
  },
};
```

`usePolling.ts`：

```ts
import { useEffect } from "react";

export function usePolling(load: () => void | Promise<void>, intervalMs: number) {
  useEffect(() => {
    void load();
    const id = window.setInterval(() => {
      if (document.visibilityState === "visible") void load();
    }, intervalMs);
    return () => window.clearInterval(id);
  }, [load, intervalMs]);
}
```

`StockTable` 使用语义化 `<table>`。第一列在同一单元格显示市场徽标、名称和代码；其余列严格按设计文档顺序。每一行使用 `onClick={() => navigate(`/stocks/${stock.market}/${stock.code}`)}` 和键盘 Enter 支持进入详情。

`StockListPage`：

- 首次加载并每 60 秒重新加载概览和当前页；
- 搜索输入 250 毫秒防抖；
- 默认每页 50 条；
- 刷新时保留旧数据；
- 加载失败显示中文错误和“重新加载”按钮；
- 过期状态显示最后成功更新时间；
- 红涨绿跌；
- 顶部概览卡展示总数、上涨、下跌、平盘和成交额。

- [ ] **步骤 4：运行列表测试和构建**

运行：

```bash
cd frontend
pnpm test -- StockListPage.test.tsx
pnpm build
```

预期：列表测试通过，构建成功。

- [ ] **步骤 5：提交股票列表页**

```bash
git add frontend/src
git commit -m "feat: 增加全市场股票列表"
```

---

### Task 9｜任务 9：个股详情、今日分钟 K 和图表缩放

**文件：**

- 创建：`frontend/src/components/KlineChart.tsx`
- 创建：`frontend/src/components/KlineChart.test.ts`
- 创建：`frontend/src/pages/StockDetailPage.tsx`
- 创建：`frontend/src/pages/StockDetailPage.test.tsx`
- 修改：`frontend/src/api/client.ts`
- 修改：`frontend/src/api/types.ts`
- 修改：`frontend/src/app/App.tsx`
- 修改：`frontend/src/app/styles.css`

**接口：**

- 消费：`GET /api/stocks/{market}/{code}`、`GET /api/stocks/{market}/{code}/bars`
- 产出：`marketApi.getStock(market, code)`、`marketApi.getBars(params)`
- 产出：`buildKlineOption(series: BarSeries): EChartsOption`
- 产出：`StockDetailPage`

- [ ] **步骤 1：写图表语义和详情周期失败测试**

`KlineChart.test.ts`：

```ts
import { buildKlineOption } from "./KlineChart";
import { todayBarsFixture } from "../test/fixtures";

test("K 线按开收低高顺序传给 ECharts 并开启双缩放", () => {
  const option = buildKlineOption(todayBarsFixture);
  const candleSeries = (option.series as Array<{ data: unknown[] }>)[0];
  expect(candleSeries.data[0]).toEqual([1334.2, 1330.06, 1330.04, 1335.08]);
  expect(option.dataZoom).toEqual(expect.arrayContaining([
    expect.objectContaining({ type: "inside" }),
    expect.objectContaining({ type: "slider" }),
  ]));
});
```

`StockDetailPage.test.tsx`：

```tsx
import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { MemoryRouter, Route, Routes } from "react-router-dom";
import { marketApi } from "../api/client";
import { dailyBarsFixture, stockDetailFixture } from "../test/fixtures";
import { StockDetailPage } from "./StockDetailPage";

vi.mock("../api/client", () => ({
  marketApi: { getStock: vi.fn(), getBars: vi.fn() },
}));

test("今日按钮请求当天原生一分钟 K", async () => {
  vi.mocked(marketApi.getStock).mockResolvedValue(stockDetailFixture);
  vi.mocked(marketApi.getBars).mockResolvedValue(dailyBarsFixture);
  render(
    <MemoryRouter initialEntries={["/stocks/SH/600519"]}>
      <Routes><Route path="/stocks/:market/:code" element={<StockDetailPage />} /></Routes>
    </MemoryRouter>,
  );
  fireEvent.click(await screen.findByRole("button", { name: "今日" }));
  await waitFor(() => expect(marketApi.getBars).toHaveBeenLastCalledWith(
    expect.objectContaining({ period: "1m", range: "today", adjustment: "none" }),
  ));
});
```

- [ ] **步骤 2：运行测试确认详情和图表缺失**

运行：

```bash
cd frontend
pnpm test -- KlineChart.test.ts StockDetailPage.test.tsx
```

预期：失败，缺少 `KlineChart` 与 `StockDetailPage`。

- [ ] **步骤 3：实现 ECharts 图表和详情页**

先扩展 `client.ts` 的 `marketApi`（保留任务 8 已有的两个方法）：

```ts
  getStock: (market: Market, code: string) =>
    request<StockQuote>(`/api/stocks/${market}/${code}`),
  getBars: (params: {
    market: Market;
    code: string;
    period: string;
    range: string;
    adjustment: string;
  }) => {
    const query = new URLSearchParams({
      period: params.period,
      range: params.range,
      adjustment: params.adjustment,
    });
    return request<BarSeries>(
      `/api/stocks/${params.market}/${params.code}/bars?${query}`,
    );
  },
```

并把 `Market`、`StockQuote`、`BarSeries` 加入 `client.ts` 顶部的类型导入。

`buildKlineOption` 必须使用以下核心配置：

```ts
export function buildKlineOption(series: BarSeries): EChartsOption {
  const categories = series.items.map((bar) => bar.bar_time);
  const candles = series.items.map((bar) => [
    bar.open_price, bar.close_price, bar.low_price, bar.high_price,
  ]);
  const volumes = series.items.map((bar) => ({
    value: bar.volume,
    itemStyle: { color: bar.close_price >= bar.open_price ? "#e5484d" : "#16a36f" },
  }));
  return {
    animation: false,
    axisPointer: { link: [{ xAxisIndex: "all" }] },
    tooltip: { trigger: "axis" },
    grid: [
      { left: 56, right: 64, top: 24, height: "62%" },
      { left: 56, right: 64, top: "74%", height: "14%" },
    ],
    xAxis: [
      { type: "category", data: categories, boundaryGap: false, gridIndex: 0 },
      { type: "category", data: categories, boundaryGap: false, gridIndex: 1, axisLabel: { show: false } },
    ],
    yAxis: [{ scale: true, gridIndex: 0 }, { scale: true, gridIndex: 1 }],
    dataZoom: [
      { type: "inside", xAxisIndex: [0, 1], start: 70, end: 100 },
      { type: "slider", xAxisIndex: [0, 1], bottom: 8, start: 70, end: 100 },
    ],
    series: [
      { name: "K 线", type: "candlestick", data: candles,
        itemStyle: { color: "#e5484d", color0: "#16a36f", borderColor: "#e5484d", borderColor0: "#16a36f" } },
      { name: "成交量", type: "bar", xAxisIndex: 1, yAxisIndex: 1, data: volumes },
    ],
  };
}
```

`KlineChart` 使用 `useRef` 保存容器，`useEffect` 创建并销毁 ECharts 实例；数据变化调用 `setOption(option, true)`；`ResizeObserver` 调用 `chart.resize()`。额外提供“−”“100%”“＋”三个按钮，通过 `dispatchAction({ type: "dataZoom", start, end })` 修改范围。

详情页周期映射固定为：

```ts
const PERIODS = [
  { label: "今日", period: "1m", range: "today", adjustment: "none" },
  { label: "1分", period: "1m", range: "5d", adjustment: "none" },
  { label: "5分", period: "5m", range: "60d", adjustment: "qfq" },
  { label: "15分", period: "15m", range: "60d", adjustment: "qfq" },
  { label: "30分", period: "30m", range: "60d", adjustment: "qfq" },
  { label: "60分", period: "60m", range: "60d", adjustment: "qfq" },
  { label: "日K", period: "1d", range: "60d", adjustment: "qfq" },
  { label: "周K", period: "1w", range: "1y", adjustment: "qfq" },
  { label: "月K", period: "1mo", range: "5y", adjustment: "qfq" },
] as const;
```

默认选择“日K”。只有“今日”处于交易时段时每 60 秒刷新。页面展示股票头部八个核心行情字段、返回全部股票和其他股票搜索入口。

- [ ] **步骤 4：运行详情测试和前端全量验证**

运行：

```bash
cd frontend
pnpm test
pnpm build
```

预期：全部前端测试通过，生产构建成功。

- [ ] **步骤 5：提交个股详情页**

```bash
git add frontend/src
git commit -m "feat: 增加个股 K 线详情页"
```

---

### Task 10｜任务 10：固定数据端到端测试、运行脚本、在线烟雾测试与文档

**文件：**

- 创建：`backend/src/a_share_radar/data_sources/fixture_source.py`
- 创建：`frontend/playwright.config.ts`
- 创建：`frontend/e2e/mvp.spec.ts`
- 创建：`scripts/bootstrap.sh`
- 创建：`scripts/dev.sh`
- 创建：`scripts/test.sh`
- 创建：`scripts/smoke_live.py`
- 创建：`README.md`
- 修改：`backend/src/a_share_radar/main.py`
- 修改：`.gitignore`

**接口：**

- 产出：`A_SHARE_FIXTURE_SOURCE=true` 时使用固定离线数据
- 产出：`./scripts/bootstrap.sh`
- 产出：`./scripts/dev.sh`
- 产出：`./scripts/test.sh`
- 产出：`A_SHARE_RUN_LIVE=1 backend/.venv/bin/python scripts/smoke_live.py`

- [ ] **步骤 1：写端到端测试**

`frontend/e2e/mvp.spec.ts`：

```ts
import { expect, test } from "@playwright/test";

test("从股票列表进入今日一分钟 K", async ({ page }) => {
  await page.goto("/");
  await expect(page.getByRole("heading", { name: "全市场行情" })).toBeVisible();
  await page.getByPlaceholder("搜索股票代码或名称").fill("贵州茅台");
  await expect(page.getByRole("row", { name: /贵州茅台/ })).toBeVisible();
  await page.getByRole("row", { name: /贵州茅台/ }).click();
  await expect(page.getByRole("heading", { name: "贵州茅台" })).toBeVisible();
  await page.getByRole("button", { name: "今日" }).click();
  await expect(page.getByText("每分钟更新")).toBeVisible();
  await expect(page.getByLabel("贵州茅台 K 线图")).toBeVisible();
});
```

`playwright.config.ts` 启动两个服务：

```ts
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "./e2e",
  use: { baseURL: "http://127.0.0.1:4173" },
  webServer: [
    {
      command: "../backend/.venv/bin/uvicorn a_share_radar.main:app --app-dir ../backend/src --port 8000",
      url: "http://127.0.0.1:8000/api/health",
      env: { A_SHARE_FIXTURE_SOURCE: "true", A_SHARE_DATA_DIR: "../data/e2e" },
      reuseExistingServer: false,
    },
    {
      command: "pnpm dev --host 127.0.0.1 --port 4173",
      url: "http://127.0.0.1:4173",
      reuseExistingServer: false,
    },
  ],
});
```

- [ ] **步骤 2：实现固定数据源并确认端到端测试先失败后通过**

`fixture_source.py` 返回两只股票、对应当前快照、60 根日 K 和当前交易日 61 根一分钟 K。所有时间固定在 `2026-08-04`，不访问网络。

`main.py` 在 `settings.fixture_source` 为真时创建 `FixtureSource`，启动时立即把固定股票、快照和 K 线写入临时数据库；固定源模式不启动公网采集调度器。

运行：

```bash
cd frontend
pnpm exec playwright install chromium
pnpm e2e
```

预期：首次在固定源接线前失败；接线完成后 `1 passed`。

- [ ] **步骤 3：实现启动、测试和在线烟雾脚本**

`scripts/bootstrap.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail
PROJECT_DIR="$(cd "$(dirname "$0")/.." && pwd -P)"
python3 -m venv "$PROJECT_DIR/backend/.venv"
"$PROJECT_DIR/backend/.venv/bin/pip" install -e "$PROJECT_DIR/backend[dev]"
pnpm --dir "$PROJECT_DIR/frontend" install
echo "依赖安装完成"
```

`scripts/dev.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail
PROJECT_DIR="$(cd "$(dirname "$0")/.." && pwd -P)"
cleanup() { kill "${BACKEND_PID:-}" "${FRONTEND_PID:-}" 2>/dev/null || true; }
trap cleanup EXIT INT TERM
"$PROJECT_DIR/backend/.venv/bin/uvicorn" a_share_radar.main:app \
  --app-dir "$PROJECT_DIR/backend/src" --reload --port 8000 &
BACKEND_PID=$!
pnpm --dir "$PROJECT_DIR/frontend" dev --host 127.0.0.1 --port 5173 &
FRONTEND_PID=$!
wait
```

`scripts/test.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail
PROJECT_DIR="$(cd "$(dirname "$0")/.." && pwd -P)"
"$PROJECT_DIR/backend/.venv/bin/pytest" "$PROJECT_DIR/backend/tests" -q
"$PROJECT_DIR/backend/.venv/bin/ruff" check "$PROJECT_DIR/backend/src" "$PROJECT_DIR/backend/tests"
pnpm --dir "$PROJECT_DIR/frontend" test
pnpm --dir "$PROJECT_DIR/frontend" build
```

`smoke_live.py` 必须先检查 `A_SHARE_RUN_LIVE == "1"`，否则以中文说明退出；启用时只调用一次 `AkshareSource.fetch_market_snapshot()`，校验返回数量大于 4000、代码唯一、贵州茅台存在，然后打印数据量和获取时间。脚本不写数据库。

- [ ] **步骤 4：编写中文 README 并运行全部验证**

README 必须包含：

- 产品截图位置与页面说明；
- Python 3.12、Node 22 以上和 pnpm 的前置要求；
- `./scripts/bootstrap.sh`、`./scripts/dev.sh`、`./scripts/test.sh`；
- 浏览器地址 `http://127.0.0.1:5173`；
- 免费数据源与限流说明；
- 全市场快照和原生分钟 K 的区别；
- 日线回补 60 个交易日、原生一分钟近期回补限制；
- 数据目录和备份方式；
- 常见问题：接口失败、数据过期、休市无更新、初始化未完成；
- 免责声明：仅供研究，不构成投资建议。

运行：

```bash
chmod +x scripts/bootstrap.sh scripts/dev.sh scripts/test.sh
./scripts/test.sh
cd frontend && pnpm e2e
git diff --check
```

预期：后端测试全部通过、Ruff 通过、前端测试全部通过、生产构建成功、端到端 `1 passed`、`git diff --check` 无输出。

- [ ] **步骤 5：执行一次显式在线烟雾测试**

运行：

```bash
A_SHARE_RUN_LIVE=1 backend/.venv/bin/python scripts/smoke_live.py
```

预期：输出全市场股票数量、获取时间和“在线行情接口可用”。若公网接口临时失败，记录实际错误；固定数据测试仍必须全部通过，不能通过放宽断言掩盖失败。

- [ ] **步骤 6：提交完整 MVP**

```bash
git add .gitignore README.md backend frontend scripts
git commit -m "feat: 完成 A 股行情系统 MVP"
git status --short --branch
```

预期：工作区干净，分支包含设计、计划和 10 个实施任务的提交历史。

---

## 实施完成后的总体验收

依次执行：

```bash
./scripts/test.sh
cd frontend && pnpm e2e
cd ..
A_SHARE_RUN_LIVE=1 backend/.venv/bin/python scripts/smoke_live.py
git log --oneline --decorate -12
git status --short --branch
```

人工验收：

1. 使用 `./scripts/dev.sh` 启动；
2. 打开 `http://127.0.0.1:5173`；
3. 确认左侧只有“全部股票”；
4. 确认一只股票只占一行；
5. 搜索代码和名称；
6. 进入贵州茅台详情页；
7. 切换“今日”，确认一根柱表示一分钟；
8. 使用加号、减号、滚轮和底部滑块缩放；
9. 切换日 K、周 K、月 K；
10. 模拟断网，确认旧数据保留且页面显示过期状态；
11. 重启系统，确认已经取得的数据仍可读取；
12. 确认任何全市场分钟快照都没有被标记为原生分钟 K。

## 官方资料

- AKShare 股票数据：<https://akshare.akfamily.xyz/data/stock/stock.html>
- FastAPI 文档：<https://fastapi.tiangolo.com/>
- DuckDB Parquet 文档：<https://duckdb.org/docs/stable/data/parquet/overview>
- Apache ECharts 能力表：<https://echarts.apache.org/en/cheat-sheet.html>
- React 文档：<https://react.dev/>
- Vite 文档：<https://vite.dev/>
